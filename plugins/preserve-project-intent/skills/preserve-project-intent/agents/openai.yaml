interface:
  display_name: "Preserve Project Intent"
  short_description: "Prevent mission drift across long, parallel AI work"
  default_prompt: "Use $preserve-project-intent to keep this project's mission, current milestone, completion boundary, and return point authoritative while the work continues."

policy:
  allow_implicit_invocation: true
